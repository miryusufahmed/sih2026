{
  "name": "maha-udyog-mitra-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Optional Express mock API for SIH26130 — mirrors the in-browser mock so the frontend can be pointed at a real backend with zero component changes.",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "node verify-backend.js"
  },
  "dependencies": {
    "better-sqlite3": "^13.0.3",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "multer": "^2.3.0"
  }
}
